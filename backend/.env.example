# Backend Environment Variables Example
# Copy this file to .env and fill in the values before running the backend server.

# MongoDB Connection String
# Replace <username>, <password>, <cluster-url>, and <dbname>
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/<dbname>?retryWrites=true&w=majority

# JWT Secret Key
# Put any long random string here
JWT_SECRET=your_jwt_secret_here

# optional: Port for the backend server and node environment for development
PORT=3000
NODE_ENV=development